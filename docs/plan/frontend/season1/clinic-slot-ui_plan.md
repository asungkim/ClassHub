# Feature: ClinicSlot(클리닉 시간표) 관리 UI

## 1. Problem Definition

- 선생님이 클리닉 시간표를 시각적으로 관리할 수 있는 UI가 필요하다.
- 주간 시간표 형태로 모든 슬롯을 한눈에 파악하고, 직관적으로 생성/수정/삭제할 수 있어야 한다.
- 시간 충돌을 사전에 방지하고, 활성/비활성 상태를 명확히 구분해야 한다.
- 사용자가 "시간표를 직접 만지는 느낌"의 인터랙션을 제공한다.

## 2. Requirements

### Functional

#### 2.1 메뉴 구조
- **'클리닉 관리'** 상위 메뉴 추가 (아코디언 형태, '초대 관리'와 동일한 UX)
- 하위 메뉴: **'클리닉 시간표'**
- 기존 '클리닉 현황' 메뉴 제거
- 추후 확장 가능한 구조 (클리닉 세션, 클리닉 기록 등)

#### 2.2 메인 화면 - 주간 시간표 뷰
- **레이아웃**: Everytime 시간표 스타일
  - 세로축: 시간 (10:00 ~ 22:00, 1시간 단위)
  - 가로축: 요일 (월~일, 7개 열)
- **슬롯 블록 표시**
  - 활성 슬롯: 파란색/초록색 배경
  - 비활성 슬롯: 회색 반투명 배경
  - 블록 내부 정보:
    - 시작~종료 시간 (예: "14:00-16:00")
    - 정원 (👥 10)
- **빈 시간대**
  - hover 시 연한 회색 배경 + "➕ 클릭하여 추가" 힌트
  - 클릭 시 생성 모달 노출 (해당 요일/시간 기본값 세팅)
- **+ 버튼** (우측 상단)
  - "➕ 새 슬롯 추가" 버튼
  - 클릭 시 빈 생성 모달 노출

#### 2.3 새 슬롯 생성 UX (드래그/롱프레스 + 모달)
- **시간대 드래그 생성 (Desktop/Tablet)**
  - 빈 시간표 영역에서 마우스를 누른 채 세로로 드래그하면 히트박스가 생기고, 드래그 종료 시 해당 요일·시간 범위를 기본값으로 갖는 생성 모달이 열린다.
  - 드래그 중에는 희미한 블록과 가이드 라인이 표시되며, 최소 30분/1시간 단위 스냅을 적용한다.
- **모바일 롱프레스 생성**
  - 터치 디바이스에서 슬롯이 없는 영역을 500ms 이상 꾹 누르면 선택 모드로 전환되고, 손가락을 위아래로 이동하며 원하는 길이를 지정할 수 있다. 손을 떼면 생성 모달이 열리고 선택한 범위가 기본값으로 들어간다.
- **모달 구조**
- **필드**
  - 요일: 드롭다운 (월~일)
  - 시작 시간: 시간 피커 (10:00 ~ 22:00)
  - 종료 시간: 시간 피커 (시작 시간 이후만 선택 가능)
  - 정원: 숫자 입력 (1~50, 기본값 10)
- **실시간 검증**
  - 시작 시간 ≥ 종료 시간 → 에러 메시지
  - 같은 요일에 시간이 겹치는 슬롯 존재 → 빨간 경고 + 저장 불가
  - 겹치는 슬롯이 있으면 "월요일 14:00-16:00에 이미 슬롯이 있습니다" 메시지
- **액션**
  - 저장: POST /api/v1/clinic-slots
  - 취소: 모달 닫기

#### 2.4 슬롯 수정/삭제 모달
- **트리거**: 시간표의 슬롯 블록 클릭
- **필드**
  - 요일: 드롭다운 (수정 가능, 겹침 체크)
  - 시작 시간: 시간 피커 (수정 가능, 겹침 체크)
  - 종료 시간: 시간 피커 (수정 가능, 겹침 체크)
  - 정원: 숫자 입력 (수정 가능)
  - 활성/비활성: 토글 스위치
- **실시간 검증**
  - 요일/시간 수정 시 다른 슬롯과 겹치면 경고
  - 자기 자신 제외하고 겹침 체크 (`excludeId` 파라미터 활용)
- **액션**
  - 저장: PATCH /api/v1/clinic-slots/{slotId}
  - 활성/비활성 토글: PATCH /api/v1/clinic-slots/{slotId}/activate or /deactivate
  - 삭제: DELETE /api/v1/clinic-slots/{slotId} (confirm 팝업 필수)
  - 취소: 모달 닫기

#### 2.5 비활성 슬롯 처리
- 비활성화된 슬롯도 시간표에 표시 (회색 반투명)
- 클릭 시 모달에서 "이 슬롯은 비활성 상태입니다" 안내
- 토글 스위치로 즉시 활성화 가능
- 완전히 숨기지 않음 (사용자가 비활성화한 슬롯 추적 가능)

#### 2.6 시간 충돌 방지 (중요!)
- **프론트엔드 사전 검증**
  1. 사용자가 요일/시간 입력 시 실시간으로 기존 슬롯과 겹침 체크
  2. 겹치는 슬롯이 있으면 입력 필드 빨간 테두리 + 에러 메시지
  3. 저장 버튼 비활성화
- **백엔드 최종 검증**
  - POST/PATCH 요청 시 409 Conflict 응답
  - 프론트에서 에러 메시지 표시

### Non-functional

- **반응형**: PC/태블릿 우선 (모바일은 가로 스크롤 허용)
- **성능**: 슬롯 데이터 캐싱 (React Query 사용)
- **접근성**: 키보드 네비게이션 지원 (Tab, Enter, Esc)
- **테스트**:
  - 타입 검증: `npm run build -- --webpack`
  - 수동 테스트: 모든 CRUD 시나리오, 시간 충돌 시나리오
- **에러 처리**: API 에러 시 사용자 친화적 메시지 표시

## 3. UI Design (Draft)

### 3.1 메뉴 구조

```
📋 대시보드
📚 강의 관리
👥 학생 관리
📅 진도 관리
🏥 클리닉 관리 (아코디언) ◀ 새로 추가
  └─ 📊 클리닉 시간표 ◀ 새로 추가
📧 초대 관리 (아코디언)
  └─ 📝 초대 목록
```

### 3.2 메인 화면 레이아웃

```
┌─────────────────────────────────────────────────────────────┐
│  클리닉 시간표                              [➕ 새 슬롯 추가]  │
├─────────────────────────────────────────────────────────────┤
│       │  월   │  화   │  수   │  목   │  금   │  토   │  일  │
├───────┼───────┼───────┼───────┼───────┼───────┼───────┼─────┤
│ 10:00 │       │       │       │       │       │       │      │
│ 11:00 │       │       │       │       │       │       │      │
│ 12:00 │       │       │       │       │       │       │      │
│ 13:00 │       │       │       │       │       │       │      │
│ 14:00 │ ┌───┐ │       │       │       │ ┌───┐ │       │      │
│       │ │14-│ │       │       │       │ │14-│ │       │      │
│ 15:00 │ │16 │ │       │       │       │ │16 │ │       │      │
│       │ │👥10│ │       │       │       │ │👥10│ │       │      │
│ 16:00 │ └───┘ │       │       │       │ └───┘ │       │      │
│ 17:00 │       │       │       │       │       │       │      │
│ 18:00 │       │       │       │       │       │ ┌───┐ │      │
│       │       │       │       │       │       │ │18-│ │      │
│ 19:00 │       │       │       │       │       │ │20 │ │      │
│       │       │       │       │       │       │ │👥15│ │      │
│ 20:00 │       │       │       │       │       │ └───┘ │      │
│ 21:00 │       │       │       │       │       │       │      │
│ 22:00 │       │       │       │       │       │       │      │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 새 슬롯 생성 모달

```
┌─────────────────────────────────────┐
│  새 슬롯 추가                    [×] │
├─────────────────────────────────────┤
│                                     │
│  요일       [월요일 ▼]              │
│                                     │
│  시작 시간  [14:00 ▼]               │
│                                     │
│  종료 시간  [16:00 ▼]               │
│                                     │
│  정원       [10      ]  (1~50)      │
│                                     │
│  ⚠️ 월요일 14:00-16:00에            │
│     이미 슬롯이 있습니다             │
│     (겹침 발생 시에만 표시)          │
│                                     │
│            [취소]  [저장]           │
└─────────────────────────────────────┘
```

### 3.4 슬롯 수정/삭제 모달

```
┌─────────────────────────────────────┐
│  슬롯 수정                       [×] │
├─────────────────────────────────────┤
│                                     │
│  요일       [월요일 ▼]              │
│                                     │
│  시작 시간  [14:00 ▼]               │
│                                     │
│  종료 시간  [16:00 ▼]               │
│                                     │
│  정원       [10      ]  (1~50)      │
│                                     │
│  활성 상태  [●─────○] ON/OFF        │
│                                     │
│                                     │
│  [🗑️ 삭제]    [취소]  [저장]        │
└─────────────────────────────────────┘
```

## 4. Component Design (Draft)

### 4.1 컴포넌트 구조

```
frontend/src/app/(dashboard)/clinic/slots/
├── page.tsx                      # 메인 페이지
├── components/
│   ├── ClinicSlotGrid.tsx        # 주간 시간표 그리드 + 드래그 핸들러
│   ├── ClinicSlotBlock.tsx       # 슬롯 블록 (클릭/드래그 감지)
│   ├── EmptyTimeSlot.tsx         # 빈 시간대 (클릭/롱프레스 이벤트)
│   ├── DragSelectionLayer.tsx    # 드래그/롱프레스 가이드 레이어
│   ├── CreateSlotModal.tsx       # 생성 모달
│   ├── EditSlotModal.tsx         # 수정/삭제 모달
│   └── SlotConflictWarning.tsx   # 겹침 경고 컴포넌트
├── hooks/
│   ├── useClinicSlots.ts         # React Query: 슬롯 목록 조회
│   ├── useCreateSlot.ts          # React Query: 슬롯 생성
│   ├── useUpdateSlot.ts          # React Query: 슬롯 수정
│   ├── useDeleteSlot.ts          # React Query: 슬롯 삭제
│   └── useSlotConflictCheck.ts   # 시간 충돌 체크 훅
└── types.ts                       # 타입 정의
```

### 4.2 주요 타입

```typescript
// frontend/src/app/(dashboard)/clinic/slots/types.ts
import { DayOfWeek } from '@/types/common';

export interface ClinicSlot {
  id: string;
  teacherId: string;
  dayOfWeek: DayOfWeek;
  startTime: string; // "HH:mm"
  endTime: string;   // "HH:mm"
  capacity: number;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
}

export interface CreateSlotFormData {
  dayOfWeek: DayOfWeek;
  startTime: string;
  endTime: string;
  capacity: number;
}

export interface UpdateSlotFormData extends CreateSlotFormData {
  isActive?: boolean;
}

export interface TimeSlotPosition {
  dayOfWeek: DayOfWeek;
  startHour: number;
  duration: number; // 시간 단위
}
```

### 4.3 API 연동

```typescript
// frontend/src/app/(dashboard)/clinic/slots/hooks/useClinicSlots.ts
import { useQuery } from '@tanstack/react-query';
import { fetcher } from '@/lib/api';
import { ClinicSlot } from '../types';

export function useClinicSlots() {
  return useQuery({
    queryKey: ['clinic-slots'],
    queryFn: async () => {
      const response = await fetcher<{ data: ClinicSlot[] }>(
        '/api/v1/clinic-slots?isActive=true'
      );
      return response.data;
    },
  });
}

// frontend/src/app/(dashboard)/clinic/slots/hooks/useCreateSlot.ts
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { fetcher } from '@/lib/api';
import { CreateSlotFormData } from '../types';

export function useCreateSlot() {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (data: CreateSlotFormData) => {
      return fetcher('/api/v1/clinic-slots', {
        method: 'POST',
        body: JSON.stringify(data),
      });
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['clinic-slots'] });
    },
  });
}

// frontend/src/app/(dashboard)/clinic/slots/hooks/useSlotConflictCheck.ts
import { useClinicSlots } from './useClinicSlots';
import { DayOfWeek } from '@/types/common';

export function useSlotConflictCheck(excludeId?: string) {
  const { data: slots = [] } = useClinicSlots();

  return (dayOfWeek: DayOfWeek, startTime: string, endTime: string) => {
    const start = timeToMinutes(startTime);
    const end = timeToMinutes(endTime);

    const conflictingSlot = slots.find(slot => {
      if (slot.id === excludeId) return false;
      if (slot.dayOfWeek !== dayOfWeek) return false;
      if (!slot.isActive) return false;

      const slotStart = timeToMinutes(slot.startTime);
      const slotEnd = timeToMinutes(slot.endTime);

      return start < slotEnd && slotStart < end;
    });

    return conflictingSlot;
  };
}

function timeToMinutes(time: string): number {
  const [hours, minutes] = time.split(':').map(Number);
  return hours * 60 + minutes;
}
```

## 5. Test Plan

### 5.1 타입 검증 (필수)
```bash
cd frontend && npm run build -- --webpack
```
- 컴파일 에러 0개 확인
- 타입 추론 정상 작동 확인

### 5.2 수동 기능 테스트 (필수)

#### 시나리오 1: 슬롯 생성
1. "➕ 새 슬롯 추가" 버튼 클릭
2. 요일, 시간, 정원 입력
3. 저장 → 시간표에 즉시 반영 확인

#### 시나리오 2: 빈 시간대 클릭 생성
1. 빈 시간대 hover → 힌트 표시 확인
2. 클릭 → 해당 요일/시간이 기본값으로 세팅된 모달 노출
3. 저장 → 시간표에 반영

#### 시나리오 3: 시간 충돌 방지
1. 이미 월요일 14:00-16:00 슬롯 존재
2. 새 슬롯 생성: 월요일 15:00-17:00 입력
3. ⚠️ 경고 메시지 표시 + 저장 버튼 비활성화 확인

#### 시나리오 4: 슬롯 수정
1. 기존 슬롯 블록 클릭 → 모달 노출
2. 요일/시간/정원 수정
3. 충돌 없으면 저장 → 시간표 업데이트 확인

#### 시나리오 5: 슬롯 활성/비활성
1. 슬롯 클릭 → 토글 스위치 OFF
2. 저장 → 슬롯이 회색 반투명으로 변경
3. 다시 클릭 → 토글 ON → 원래 색으로 복구

#### 시나리오 6: 슬롯 삭제
1. 슬롯 클릭 → "🗑️ 삭제" 버튼 클릭
2. Confirm 팝업 → 확인
3. 시간표에서 슬롯 제거 확인

#### 시나리오 7: 에러 처리
1. API 에러 시뮬레이션 (네트워크 차단)
2. 에러 메시지 표시 확인
3. 재시도 가능 확인

### 5.3 엣지 케이스 검증 (권장)

- 슬롯 0개 상태 (빈 시간표)
- 슬롯 100개 상태 (성능 확인)
- 모든 요일/시간대에 슬롯 가득 찬 상태
- 비활성 슬롯만 있는 상태
- 동일 시간대에 여러 슬롯 생성 시도 (연속 클릭)

### 5.4 반응형 테스트

- PC (1920×1080): 정상 표시
- 태블릿 (768×1024): 가로 스크롤 허용
- 모바일 (375×667): 가로 스크롤 + 축소 표시

## 6. 구현 순서

### Phase 1: 메뉴 추가
1. ✅ NavigationBar에 '클리닉 관리' 아코디언 메뉴 추가
2. ✅ 하위 메뉴 '클리닉 시간표' 추가
3. ✅ 라우팅 설정: `/clinic/slots`

### Phase 2: 빈 시간표 레이아웃
1. ✅ `ClinicSlotGrid` 컴포넌트 작성 (10:00~22:00, 월~일)
2. ✅ 시간축, 요일축 레이블 표시
3. ✅ 그리드 스타일링 (테두리, hover 효과)

### Phase 3: API 연동 & 슬롯 표시
1. ✅ React Query 훅 작성 (`useClinicSlots`)
2. ✅ `ClinicSlotBlock` 컴포넌트 작성 (시간, 정원 표시)
3. ✅ 시간표에 슬롯 블록 배치 (위치 계산 로직)
4. ✅ 활성/비활성 색깔 구분

### Phase 4: 슬롯 생성
1. ✅ `CreateSlotModal` 컴포넌트 작성
2. ✅ "+ 버튼" 클릭 → 모달 노출
3. ✅ 빈 시간대 클릭 → 모달 노출 (기본값 세팅)
4. ✅ `useCreateSlot` 훅 연동
5. ✅ 시간 충돌 체크 (`useSlotConflictCheck`)

### Phase 5: 슬롯 수정/삭제
1. ✅ `EditSlotModal` 컴포넌트 작성
2. ✅ 슬롯 블록 클릭 → 모달 노출
3. ✅ `useUpdateSlot`, `useDeleteSlot` 훅 연동
4. ✅ 토글 스위치 연동 (활성/비활성)
5. ✅ 삭제 confirm 팝업

### Phase 6: 검증 & 테스트
1. ✅ `npm run build -- --webpack` 실행 → 타입 에러 0개
2. ✅ 모든 CRUD 시나리오 수동 테스트
3. ✅ 시간 충돌 시나리오 테스트
4. ✅ 에러 처리 테스트

### Phase 7: 최적화 & 마무리
1. ✅ 로딩 상태 처리 (Skeleton UI)
2. ✅ 에러 바운더리 추가
3. ✅ 반응형 확인
4. ✅ AGENT_LOG 기록

## 7. 추가 고려사항

### 7.1 UX 개선
- 슬롯 생성 시 애니메이션 (fade-in)
- 슬롯 삭제 시 애니메이션 (fade-out)
- 드래그로 시간 범위 선택 (고급 기능, 추후 구현)

### 7.2 접근성
- 키보드 네비게이션 (Tab, Enter, Esc)
- 스크린 리더 지원 (aria-label)
- 포커스 표시 (outline)

### 7.3 성능
- React Query 캐싱 (5분)
- 슬롯 블록 메모이제이션 (React.memo)
- 가상 스크롤 (슬롯이 많을 경우)

---

이 계획이 승인되면 Phase별로 순차 구현을 시작합니다.
